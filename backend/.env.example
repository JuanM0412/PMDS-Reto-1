# Copy this file to .env and fill in your values.

# ── General ────────────────────────────────────────────────────────────────
APP_NAME=agents-backend
ENVIRONMENT=development
DEBUG=false

# ── CORS ───────────────────────────────────────────────────────────────────
# Comma-separated list of allowed origins (JSON array syntax for pydantic)
CORS_ALLOW_ORIGINS=["http://localhost:5173"]

# ── PostgreSQL ─────────────────────────────────────────────────────────────
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/agents_db

# ── N8N ────────────────────────────────────────────────────────────────────
N8N_BASE_URL=http://localhost:5678
# N8N_API_KEY=your-n8n-api-key   # Uncomment if n8n requires authentication
N8N_REQUEST_TIMEOUT=90.0

# ── Public URL of this backend ─────────────────────────────────────────────
# This URL is forwarded to n8n so it knows where to send the callback.
# If n8n runs in Docker on Linux, use the Docker gateway IP (172.17.0.1)
# instead of localhost so the container can reach the host:
#   PUBLIC_BASE_URL=http://172.17.0.1:8000
# Also make sure uvicorn listens on 0.0.0.0, not 127.0.0.1:
#   uvicorn app.main:app --host 0.0.0.0 --port 8000
PUBLIC_BASE_URL=http://172.17.0.1:8000
